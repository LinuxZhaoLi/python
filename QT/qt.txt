1 信号操
    connect(b4,&QPushButton::released,
    // = 把类中的所有成员以值传递的方式， 局部变量
    // & 引用方式
    [=]{

    qDebug()<<""<<endl;
    }
    )

    信号属于谁的，是谁发出来的。

1 问题：
    The X11 connection broke: I/O error (code 1)
    XIO:  fatal IO error 11 (资源暂时不可用) on X server ":0"
          after 12780279 requests (12780279 known processed) with 0 events remaining.


    两个线程都涉及到界面的问题，并且我并没有考虑同步的问题，所以导致了标题中所说的问题。

    说是由于两个线程抢占GUI资源导致的，去除其中一个线程中的控件句柄，说白了就是只能有一个线程来画界面。

1 数据转化：
    QVariantMap 转化为QVariantList 
      //{} --> []
            QVariantList ll;
            QList<QString> lis= data.keys();
            for (int i = 0;i<lis.length();i++ )
            {
                QVariantMap mm;
                mm[lis[i]] = data[lis[i]].toInt();
                ll.append(mm);

            }



1 对齐方式：
    verticalAlignment: Text.AlignVCenter
2 字体集合
    Qt.fontFamilies()
3 定义小数
     property real fontX: 0.55
4 定义信号
    signal evtQml(int id, variant params);
5 定义数组
    var words = new Array();
6 消除重影
    antialiasing: true 消除混叠现象

7 定时器
    Timer{
        id: id
        running: isRun ? true:false
        interval:3000
        onTriggered{

        // do something

        }
    }
8 动画
    NumberAnimation{
        id: id
        target: xx
        running: false
        property: "x"
        duration: 2000 时间
        onStopped:{

        }

   }

9 qml 获取时间
    var dt=newDate;
    var tt = Qt.formatDateTime("hh:mm:ss")
10 map转化为string
    QJsonDocument json = QJsonDocument::fromVariant(op);
    QString str = json.toJson();

11 列表数据再界面上显示：
    （1）后端传过来，列表数据： ll， 
    （2）自定义组件： 